
# === Required for summarization ===
GROQ_API_KEY=replace_me

# Optional: override model name (defaults to llama-3.1-70b-versatile)
# GROQ_MODEL=llama-3.1-8b-instant

# === SMTP settings for Nodemailer ===
# Use any SMTP you have access to (e.g., Gmail SMTP with app password, Mailgun, SendGrid, SES, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_FROM="Meeting Bot <no-reply@example.com>"
SMTP_SECURE=false

# Port for the server
PORT=3000
